/** Author: Alexander Bech* Leetcode Problem Solution* Leetcode ID: 11
*/#include <iostream>#include <string>#include <vector>class Solution {public:    int area(int a, int b)    {        return a*b;    }        int min(int a, int b)    {        return a > b ? b : a;    }        int maxArea(std::vector<int>& height)    {        int _width;        int _height;        int _area;        int _max = 0;        for (int i = 0; i < height.size(); i++)        {            for (int j = i + 1; j < height.size(); j++)            {                _width = j - i;                _height = min(height[i],height[j]);                _area =  area(_width,_height);                if (_area > _max) _max = _area;            }        }                return _max;        }        };/* Testing */int main(){}