/** Author: Alexander Bech* Leetcode Problem Solution* Leetcode ID: 20
*/#include <iostream>#include <vector>#include <string>class Solution {public:bool isValid(std::string s){	std::vector<char> arrS(s.begin(),s.end());    std::vector<int> idx(3,0);    const std::string start = "{[(";    const std::string end = "}])";    for (const auto element : arrS)    {	auto _start = start.find(element);	auto _end = end.find(element);            if (_start != std::string::npos)      {        idx[_start]++;        if ((idx[_start] != 1))          return false;      }      else      {        idx[_end]--;        if ((idx[_end] != 0))          return false;      }    }  	for (const auto element : idx)	  {		if (element != 0)		  return false;	  }  	return true;}};/* Testing */int main(){	std::cout << "hello world" << std::endl;}