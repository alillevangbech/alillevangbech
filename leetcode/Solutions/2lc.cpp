/** Author: Alexander Bech* Leetcode Problem Solution* Leetcode ID: 2
*/#include <iostream>struct ListNode {    int val;    ListNode *next;    ListNode(int x) : val(x), next(NULL) {}};class Solution {public:    ListNode* addTwoNumbers(ListNode* l1, ListNode* l2){	ListNode res(0);	ListNode* p_res = &res;	ListNode* p_iter = p_res;	int sum = 0;		while (l1 != NULL || l2 != NULL)	{		sum /= 10;		if (l1 != NULL)		{			sum += l1->val;			l1 = l1->next;		}		if (l2 != NULL)		{			sum += l2->val;			l2 = l2->next;		}				p_iter->next = new ListNode(sum % 10);        p_iter = p_iter->next;	}	if (sum/10 == 1)		p_iter->next = new ListNode(1);	return p_res->next;}};/* Testing */int main(){}